(ns app.petit-plat.password-hash.interface-test
  (:require [clojure.test :as test :refer :all]
            [app.petit-plat.password-hash.interface :as password-hash]))

(deftest dummy-test
  (is (= 1 1)))
