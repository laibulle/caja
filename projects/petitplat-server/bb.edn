{:tasks {test   (shell "clj -X:test")
         format:check   (shell "cljfmt check")
         format         (shell "cljfmt fix")
         gather-migrations
         {:doc "Gather and process migrations"
          :task (do
                  (require '[petitplat.server.migrate :refer [gather-migrations]])
                  (gather-migrations))}}}